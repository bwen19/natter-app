enum UserRole { user, admin }

enum RoomCategory { personal, private, public }

enum MessageKind { text, image, file }

enum MemberRank { owner, member }

enum FriendStatus { adding, accepted, deleted }

enum UserRelation { userself, friend, incoming, outgoing, stranger }
